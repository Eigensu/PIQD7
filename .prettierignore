apps/web/next-env.d.ts

# Machine-generated: its exact serialization varies between pnpm releases, so
# formatting it would make `format:check` depend on who ran `pnpm install` last.
pnpm-lock.yaml

# PIQD7's original standalone pages, kept exactly as authored. Prettier would
# reflow their hand-tuned markup, so they stay out of the formatter.
/palette-options.html
/piqd-landing.html
